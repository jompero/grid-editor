{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:ui": "del /s /q public && cd ..\\client && npm run build --prod && xcopy build ..\\backend\\public\\ /s",
    "start": "node ./bin/www",
    "watch": "nodemon ./bin/www"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
